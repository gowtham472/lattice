[package]
name = "lattice-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "The LATTICE pipeline: collect, normalise, classify, graph, assess, report"

[dependencies]
lattice-cbom = { path = "../lattice-cbom" }
lattice-classify = { path = "../lattice-classify" }
lattice-collectors = { path = "../lattice-collectors" }
lattice-core = { path = "../lattice-core" }
lattice-graph = { path = "../lattice-graph" }
lattice-risk = { path = "../lattice-risk" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
toml.workspace = true
tracing.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
